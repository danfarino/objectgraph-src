#!/bin/bash

# Run this to build the Web Worker for the Monaco editor

# You'll need to do "npm install" in this directory (separate from the parent directory)
# before this will work.

./node_modules/.bin/webpack-cli
